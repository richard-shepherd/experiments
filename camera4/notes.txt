- Options for how to show the video
    var options = {
        width: width,
        height: height,
        facingDirection: enum,
        showVideo: videoElementID,
        showCanvas: {
            canvasElementID: canvasElement,
            sampleIntervalMilliseconds: 33
        }
    }

- Do you ever want to show the canvas?
  - Yes: So that you can draw on it.